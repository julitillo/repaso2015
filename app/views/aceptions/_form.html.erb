<%= form_for(@aception) do |f| %>
  <% if @aception.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aception.errors.count, "error") %> prohibited this aception from being saved:</h2>

      <ul>
      <% @aception.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequencial %><br>
    <%= f.number_field :sequencial %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :word_id %><br>
    <%= f.text_field :word_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
