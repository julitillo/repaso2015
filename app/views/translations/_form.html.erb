<%= form_for(@translation) do |f| %>
  <% if @translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translation.errors.count, "error") %> prohibited this translation from being saved:</h2>

      <ul>
      <% @translation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aception_own_id %><br>
    <%= f.text_field :aception_own_id %>
  </div>
  <div class="field">
    <%= f.label :aception_for_id %><br>
    <%= f.text_field :aception_for_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
