<%= @ask%>
<%= form_for(@ask) do |f| %>
  <% if @ask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ask.errors.count, "error") %> prohibited this ask from being saved:</h2>

      <ul>
      <% @ask.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label @ask.aception_own.word.name %><br>
    (<%= f.label @ask.aception_own.name %>)<br>
  </div>
  <div class="field">
    <%= f.label 'The correct translation is:' %><br>
    <%= f.text_field :answerfor %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
