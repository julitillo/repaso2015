<%= form_for(@translations_versions_test) do |f| %>
  <% if @translations_versions_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translations_versions_test.errors.count, "error") %> prohibited this translations_versions_test from being saved:</h2>

      <ul>
      <% @translations_versions_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :translations_versions_id %><br>
    <%= f.text_field :translations_versions_id %>
  </div>
  <div class="field">
    <%= f.label :test_id %><br>
    <%= f.text_field :test_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
